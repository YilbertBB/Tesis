import 'package:flutter/material.dart';

class UpgradeProduct extends StatelessWidget {
  const UpgradeProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}